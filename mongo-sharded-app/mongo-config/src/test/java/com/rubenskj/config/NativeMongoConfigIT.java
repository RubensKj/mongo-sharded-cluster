package com.rubenskj.config;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMongoConfigIT extends MongoConfigTest {

    // Execute the same tests but in native mode.
}